document.getElementById('contactForm').addEventListener('submit', function(event) {
  event.preventDefault();
  // You can add further logic here, e.g., sending form data to a server
  alert('Form submitted!');
});
